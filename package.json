{
  "name": "g_drive_file_combine",
  "version": "1.0.0",
  "description": "When we download the large file, internet break it down to folders and download it as zip. This script will combine all the folder's content into one.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index"
  },
  "author": "Swetha Selvakumaran",
  "license": "ISC",
  "dependencies": {
    "adm-zip": "^0.5.16",
    "fs": "^0.0.1-security",
    "node-stream-zip": "^1.15.0",
    "unzipper": "^0.12.3"
  }
}
